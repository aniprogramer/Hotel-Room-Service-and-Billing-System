<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/header') %>
    <title>Confirm Order | Hotel Service</title>
  </head>
  <body class="bg-light">
    <%- include('../partials/navbar',{user,active:'menu'}) %>

    <div class="container py-5">
      <h4 class="text-center fw-bold text-primary mb-4">🧾 Confirm Your Order</h4>

      <div class="card shadow border-0">
        <div class="card-body">
          <table class="table table-bordered text-center align-middle">
            <thead class="table-primary">
              <tr><th>Item</th><th>Qty</th><th>Price</th><th>Subtotal</th></tr>
            </thead>
            <tbody>
              <% let total=0; items.forEach(it=>{let sub=it.price*it.quantity; total+=sub;%>
              <tr>
                <td><%=it.name%></td><td><%=it.quantity%></td>
                <td>₹<%=it.price.toFixed(2)%></td><td>₹<%=sub.toFixed(2)%></td>
              </tr>
              <%})%>
            </tbody>
          </table>
          <div class="text-end pe-3">
            <h6>Subtotal: ₹<%=total.toFixed(2)%></h6>
            <h6>Tax (5%): ₹<%=(total*0.05).toFixed(2)%></h6>
            <h5 class="fw-bold text-primary">Total: ₹<%=(total*1.05).toFixed(2)%></h5>
          </div>

          <div class="text-center mt-3">
            <form action="/guest/order/confirm" method="POST">
              <button class="btn btn-success px-4">Confirm Order</button>
            </form>
          </div>
        </div>
      </div>
    </div>

    <%- include('../partials/footer') %>
  </body>
</html>
