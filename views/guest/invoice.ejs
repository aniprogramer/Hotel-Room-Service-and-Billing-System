<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/header') %>
    <title>Invoice | Hotel Service</title>
  </head>
  <body class="bg-light">
    <%- include('../partials/navbar',{active:'orders'}) %>

    <div class="container py-5">
      <div class="card shadow border-0 mx-auto" style="max-width:700px;">
        <div class="card-body">
          <h3 class="text-center fw-bold text-primary mb-4">🧾 Invoice</h3>

          <div class="row mb-3">
            <div class="col-md-6">
              <p><strong>Invoice No:</strong> <%=invoice.invoice_number%> </p>
              <p><strong>Guest:</strong> <%=invoice.full_name%></p>
            </div>
            <div class="col-md-6 text-end">
              <p><strong>Room:</strong> <%=invoice.room_number%></p>
              <p><strong>Status:</strong>
                <span class="badge bg-<%=invoice.paid?'success':'danger'%>">
                  <%=invoice.paid?'Paid':'Unpaid'%>
                </span>
              </p>
            </div>
          </div>

          <table class="table table-bordered">
            <tbody>
              <tr><th>Subtotal</th><td>₹<%=invoice.subtotal.toFixed(2)%></td></tr>
              <tr><th>Tax</th><td>₹<%=invoice.tax.toFixed(2)%></td></tr>
              <tr><th>Service Charge</th><td>₹<%=invoice.service_charge.toFixed(2)%></td></tr>
              <tr class="table-primary"><th>Total</th><td>₹<%=invoice.total_amount.toFixed(2)%></td></tr>
            </tbody>
          </table>

          <div class="text-center mt-4">
            <a href="/guest/orders" class="btn btn-outline-primary px-4">Back to Orders</a>
          </div>
        </div>
      </div>
    </div>

    <%- include('../partials/footer') %>
  </body>
</html>
